package ${viperPackage}.view.adapter.agregate

import ${viperPackage}.entity.${prefix}

class ${prefix}${aggregateName}Item(m${prefix}: ${prefix}) : ${aggregateName}Item {

	var TYPE: Int = ${prefix}${aggregateName}Item.class.hashCode();
}

